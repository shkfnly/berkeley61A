def take_color(self, color):
  for s in self.skittles:
    if s.color == color:
      return take_skittle()
  return None

def take_all(self):
  
  for i in range(len(self.skittles)):
    print self.skittles[i].color
    curr_skittle = take_skittle()\

  for i in range(len(self.skittles)):
    print (self.take_skittle().color)